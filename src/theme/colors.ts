import { blue } from '@mui/material/colors';

const colors = {
  primary: blue[700],
  secondary: '#000000',
  paperLight: '#ffffff',
  bgLight: '#eeeeee',
  lightGray: '#ededed',
};

export default colors;
